{
  "mcpServers": {
    "sysspec": {
      "command": "uvx",
      "args": ["--from", "${CLAUDE_PLUGIN_ROOT}/kit", "sysspec-mcp"],
      "env": {
        "SPECS_DIR": "${CLAUDE_PLUGIN_ROOT}/specs"
      }
    }
  }
}
