{
  "mcpServers": {
    "catalog": {
      "command": "uvx",
      "args": ["--from", "${CLAUDE_PLUGIN_ROOT}/kit", "contracts-mcp"],
      "env": {
        "CATALOG_DIR": "${CLAUDE_PLUGIN_ROOT}/catalog"
      }
    }
  }
}
