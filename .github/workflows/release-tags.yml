name: release-tags

on:
  push:
    branches: [main]
    paths: ["specs/**"]
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  tag:
    uses: ./.github/workflows/sysspec-release-tags.yml
