name: release-tags

on:
  push:
    branches: [main]
    paths: ["catalog/**"]
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  tag:
    uses: ./.github/workflows/catalog-release-tags.yml
